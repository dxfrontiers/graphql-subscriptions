
query fetchTimeline {
    timeline {
        id
        message
        postedAt
        user {
            displayName
            username
        }
        replies {
            id
        }
    }
}
