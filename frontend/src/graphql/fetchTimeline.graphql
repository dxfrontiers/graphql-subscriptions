
query fetchTimeline {
    timeline {
        id
        message
        postedAt
        user {
            displayName
            username
        }
        replies {
            id
            message
            user {
                displayName
                username
            }
            postedAt
        }
    }
}
